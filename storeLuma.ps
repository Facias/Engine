#version 400
layout(location = 0) out vec4 color;


uniform sampler2D texture0;

in vec2 uvVarying;


void main()
{
	vec4 tex = texture2D(texture0, uvVarying);	
	tex.a = dot(tex.rgb, vec3(0.299, 0.587, 0.114));

	color = tex;
	//color = vec4(0,0,0,.5);

}