#version 400 core
layout(location = 0) out vec4 color;

in vec2 uvVarying;
uniform vec4 vcolor;

void main()
{
	color = vcolor;
	color.a = 1.0-abs(2.0*uvVarying.x-1.0);
	gl_FragDepth = gl_FragCoord.z-.00001;
	
}